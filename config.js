window.DMI_HS_UPDATER_CONFIG = Object.freeze({
  // This public JSON file is published with the DMI-HS binaries.
  // Change only this URL when a different Firebase project or domain is used.
  releaseUrl: "https://dmi-hs.web.app/firmware/release.json"
});
